<?xml version="1.0" encoding="UTF-8"?>
<project>
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId> be.tr.democracy</groupId>
        <artifactId>voting-application-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <packaging>jar</packaging>
    <name>use-cases</name>
    <artifactId>use-cases</artifactId>
    

    <description>Module that contains the use cases, the commands that contain the orchestration logic that trigger the domain models.</description>

   
    <dependencies>
       
    </dependencies>

    <build>
        <plugins>
           
        </plugins>
    </build>
</project>
